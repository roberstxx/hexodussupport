import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Asistencia | Huella",
}

export default function HuellaLayout({ children }: { children: React.ReactNode }) {
  return children
}
